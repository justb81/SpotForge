import { registerRootComponent } from "expo";

import App from "./App";

// registerRootComponent registriert die Wurzelkomponente sowohl für Expo Go
// als auch für native Builds.
registerRootComponent(App);
