// Wurzel-ESLint-Config. Re-exportiert das geteilte Preset aus @spotforge/config,
// das via Verzeichnis-Kaskade auch für alle Pakete/Apps gilt (`eslint .`).
export { default } from "@spotforge/config/eslint";
