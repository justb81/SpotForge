{
  "name": "@spotforge/app-config",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "echo \"TODO: tsc\" && exit 0",
    "lint": "echo \"TODO: eslint\" && exit 0",
    "test": "echo \"TODO: vitest\" && exit 0",
    "typecheck": "echo \"TODO: tsc --noEmit\" && exit 0"
  },
  "dependencies": {
    "@spotforge/game-core": "workspace:*"
  }
}
