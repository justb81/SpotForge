{
  "$schema": "https://json.schemastore.org/tsconfig",
  "//": "Node-Anwendungen (backend). Build via tsup; tsc dient nur dem Typecheck.",
  "extends": "./base.json",
  "compilerOptions": {
    "lib": ["ES2022"],
    "types": ["node"]
  }
}
