# Python-Abhängigkeiten der Export-Pipeline (läuft in CI / lokal, NICHT im
# Mobile-Bundle). optimum-executorch zieht passendes torch/executorch/transformers
# selbst nach – daher hier bewusst nur die direkten Tools gepinnt.
optimum-executorch==1.1.0
# Keine obere Schranke: das von optimum-executorch gezogene transformers verlangt
# huggingface_hub>=1.2 – ein <1.0-Pin würde die Auflösung blockieren.
huggingface_hub>=0.27
# timm-Backend (z.B. das ImageNet-Gate EfficientNet-B0 #83 oder Jordo23/vehicle-
# classifier, EfficientNet-B4). Nutzt das von optimum-executorch gezogene torch.
timm>=1.0,<2.0
# Pillow: nur für den Off-Device-Pre-Screen (prescreen.py) zum Laden der Eval-Bilder.
Pillow>=10.0,<12.0
